<?xml version='1.0' standalone='yes'?>
<!-- Config version G99 -->
<admin>
	<parameters />
	
	<users>
		<!-- 
		<authorized>
			<user username="web100218l" withChildrenUsers="0" onlyChildrenUsers="0" />
		</authorized> 
		-->
		
		<forbidden>
			<!-- <user username="web100218l" withChildrenUsers="0" onlyChildrenUsers="1" /> -->
		</forbidden>
		
		<!-- 
		<public>
			<user username="Start" isDefaultLogin="1">
				<label_l01><img src="images/icones/32x32/card.png"/></label_l01><label_l02><img src="images/icones/32x32/card.png"/></label_l02>
			</user>
		</public>
		-->
	</users>
	
	<!-- Medair (CWE) 04.04.2017 : If EmailService_sendOnBehalfOfUser = true, then defines an optional list of emails authorized to be used by the SMTP account to directly send from. --> 
	<!-- If a username is defined, then the EmailService only sends from the given email address if the email message was created by the associated user -->
	<!-- Else the email message is not sent and has an error status -->
	<!-- 
	<Emailing>
		<authorizedDirectSenders>
			<user email="user1@xxx.xx" username="user1" />
			<user email="user2@xxx.xx" username="user2" />
			<user email="user3@xxx.xx, user3_1@xxx.xx" username="user3" />
			<user email="group1@xxx.xx, group2@xxx.xx" />
		</authorizedDirectSenders>
	</Emailing>	
	-->
	
	<!-- CWE 11.01.2019 : Declares a list of cross origin referers which are authorized to call some Wigii web actions -->
	<!-- By default Wigii web actions are only authorized for site root itself, except for /public action authorized to anyone -->
	<!-- 
	<CrossOrigin>
		<authorizedReferers>
			<client1 hostUrl="company.wigii-safe.com" actions="fx"/>
		</authorizedReferers>
	</CrossOrigin>
	-->
</admin>